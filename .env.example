# ---------------------------------------------------------------
#    .env.example (v 2.0)
# ---------------------------------------------------------------
# Copia esto en tu archivo llamado '.env.example'

# Base de datos (usadas por docker-compose)
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URL de conexión ASÍNCRONA (¡Importante el +asyncpg!)
# Nota: Esta línea es solo de referencia, el docker-compose.yml
# la construye dinámicamente para el contenedor 'web'.
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# JWT Configuration
JWT_SECRET_KEY=flyblue_secret_key_2024_change_this_in_production